name: Playwright Tests

on:
  push:
    branches: [main, develop]

  merge_group:
    branches: [main, develop]

  # re-register cron
  workflow_dispatch:

  # 👉 Cron chạy mỗi ngày
  schedule:
    - cron: "0 1 * * *"

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    # 👉 Cron luôn dùng staging (tránh bị hiểu nhầm là main)
    # ✅ FIX: dùng github.ref thay vì github.ref_name
    # ✅ FIX: viết 1 dòng để tránh lỗi YAML
    environment: ${{ github.event_name == 'schedule' && 'staging' || github.ref == 'refs/heads/main' && 'production' || github.ref == 'refs/heads/develop' && 'staging' }}

    # 👉 Chia test thành 4 shard để chạy song song
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]

    env:
      ENV: ${{ vars.ENV }}
      BASE_URL: ${{ vars.BASE_URL }} # ✅ NEW: Base URL cho Playwright (production/staging tùy environment)
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}

    steps:
      # 👉 Cron checkout develop
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'schedule' && 'develop' || github.ref_name }}

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # 👉 Chạy test theo shard + tạo blob report để merge sau
      - name: Run Playwright tests (Shard ${{ matrix.shard }})
        run: |
          npx playwright test \
            --shard=${{ matrix.shard }}/4 \
            --reporter=blob \
            --retries=1

      # 👉 Upload blob report tạm thời (dùng để merge)
      - name: Upload blob report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: blob-report-${{ matrix.shard }}
          path: blob-report
          retention-days: 1


  # 👉 Merge tất cả shard thành 1 HTML report duy nhất
  merge-report:
    if: always()
    needs: test
    runs-on: ubuntu-latest

    # ✅ EXPORT SUMMARY SAU MERGE (matrix không export được)
    outputs:
      passed: ${{ steps.summary.outputs.passed }}
      failed: ${{ steps.summary.outputs.failed }}
      skipped: ${{ steps.summary.outputs.skipped }}

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      # 👉 Download toàn bộ blob report từ các shard
      - name: Download blob reports
        uses: actions/download-artifact@v4
        with:
          path: all-blob-reports

      # 👉 FIX: Flatten tất cả file blob report vào 1 folder chung
      - name: Prepare blob reports
        run: |
          mkdir merged-blobs
          find all-blob-reports -type f -exec cp {} merged-blobs/ \;

      # 👉 Merge thành HTML duy nhất
      - name: Merge reports thành HTML duy nhất
        run: |
          npx playwright merge-reports \
          --reporter html,json \
          ./merged-blobs


      # ✅ Extract summary từ report đã merge (FULL DATA)
      - name: Extract final summary
        id: summary
        run: |
          if [ -f playwright-report/test-results.json ]; then
            PASSED=$(jq '[.suites[].specs[].tests[] | select(.status=="passed")] | length' playwright-report/test-results.json)
            FAILED=$(jq '[.suites[].specs[].tests[] | select(.status=="failed")] | length' playwright-report/test-results.json)
            SKIPPED=$(jq '[.suites[].specs[].tests[] | select(.status=="skipped")] | length' playwright-report/test-results.json)
          else
            PASSED=0
            FAILED=0
            SKIPPED=0
          fi

          echo "passed=$PASSED" >> $GITHUB_OUTPUT
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "skipped=$SKIPPED" >> $GITHUB_OUTPUT

          
  # 👉 Deploy report lên GitHub Pages (chỉ main)
  deploy-report:
    needs: merge-report
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write

    steps:
      - name: Download final report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report-${{ vars.ENV }}
          path: ./public

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4


  notify-slack:
    needs: [merge-report, deploy-report]
    runs-on: ubuntu-latest
    if: always()
    continue-on-error: true
    environment: slack

    steps:
    - name: Send Slack notification
      run: |
        RESULT="${{ needs.merge-report.result }}"

        if [ "$RESULT" = "success" ]; then
          STATUS="PASSED"
          COLOR="#36a64f"
          ICON="✅"
        else
          STATUS="FAILED"
          COLOR="#ff0000"
          ICON="❌"
        fi

        PASSED="${{ needs.merge-report.outputs.passed }}"
        FAILED="${{ needs.merge-report.outputs.failed }}"
        SKIPPED="${{ needs.merge-report.outputs.skipped }}"

        ACTION_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        REPORT_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"

        WEBHOOK="${{ secrets.SLACK_WEBHOOK_URL }}"

        curl -X POST \
          -H "Content-type: application/json" \
          --data "{
            \"attachments\": [
              {
                \"color\": \"$COLOR\",
                \"title\": \"$ICON Playwright CI $STATUS\",
                \"fields\": [
                  {\"title\": \"Repository\", \"value\": \"${{ github.repository }}\", \"short\": true},
                  {\"title\": \"Branch\", \"value\": \"${{ github.ref_name }}\", \"short\": true},
                  {\"title\": \"Passed\", \"value\": \"$PASSED\", \"short\": true},
                  {\"title\": \"Failed\", \"value\": \"$FAILED\", \"short\": true},
                  {\"title\": \"Skipped\", \"value\": \"$SKIPPED\", \"short\": true},
                  {\"title\": \"Actions\", \"value\": \"$ACTION_URL\", \"short\": false},
                  {\"title\": \"Report\", \"value\": \"$REPORT_URL\", \"short\": false}
                ]
              }
            ]
          }" \
          "$WEBHOOK"


  notify-telegram:
    needs: [merge-report, deploy-report]
    runs-on: ubuntu-latest
    if: always()
    continue-on-error: true

    steps:
      - name: Send Telegram notification
        run: |
          RESULT="${{ needs.merge-report.result }}"

          if [ "$RESULT" = "success" ]; then
            STATUS="PASSED"
            ICON="✅"
          else
            STATUS="FAILED"
            ICON="❌"
          fi

          PASSED="${{ needs.merge-report.outputs.passed }}"
          FAILED="${{ needs.merge-report.outputs.failed }}"
          SKIPPED="${{ needs.merge-report.outputs.skipped }}"

          ACTION_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          REPORT_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"

          MESSAGE=$(cat <<EOF
          🎭 Playwright CI $STATUS $ICON

          Repository: ${{ github.repository }}
          Branch: ${{ github.ref_name }}

          📊 Test Summary
          • Passed: $PASSED
          • Failed: $FAILED
          • Skipped: $SKIPPED

          🔗 Actions: $ACTION_URL
          📈 Report: $REPORT_URL
          EOF
          )

                    curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
                      -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                      --data-urlencode text="$MESSAGE"
